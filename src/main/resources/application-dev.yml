app:
  env: dev

logging:
  config: classpath:log4j2-dev.xml

spring:
  ## datasource
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://192.168.0.114/open_mediation?useCursorFetch=true&characterEncoding=utf8&useSSL=true&requireSSL=false&allowLoadLocalInfile=true
    username: root
    password: NPN_loczzf2_root
  ## thymeleaf
  thymeleaf:
    cache: false # 开发时关闭缓存,不然没法看到实时页面

# currency api key from https://fixer.io
currency.api.appkey: REPLACE_YOUR_APPKEY